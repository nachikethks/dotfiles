# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# EDIT THIS CONFIG ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################



# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hypr.land/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hypr.land/Configuring/Monitors/
monitor=DP-1,1920x1080@144,auto,1
monitor=eDP-1,1920x1080,auto,1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = nautilus
$menu = rofi -show drun


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:
# exec-once = swaync

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
exec-once = swww-daemon
exec-once = waybar
exec-once = swaync
exec-once = hyperidle
exec-once = wl-clip-persist --clipboard both
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
exec-once = systemctl --user start hyprpolkitagent
exec-once = /usr/bin/pypr
exec-once = hyprlock


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hypr.land/Configuring/Environment-variables/

env = XCURSOR_THEME,Breeze_Snow
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = GDK_BACKEND,wayland
env = CLUTTER_BACKEND,wayland


###################
### PERMISSIONS ###
###################

# See https://wiki.hypr.land/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 20

    border_size = 2

    # https://wiki.hypr.land/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgb(89b4fa)
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hypr.land/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hypr.land/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default curves, see https://wiki.hypr.land/Configuring/Animations/#curves
    #        NAME,           X0,   Y0,   X1,   Y1
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    # Default animations, see https://wiki.hypr.land/Configuring/Animations/
    #           NAME,          ONOFF, SPEED, CURVE,        [STYLE]
    animation = global,        1,     10,    default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     4.79,  easeOutQuint
    animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     4,     easeOutQuint, fade
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,    1,     1.94,  almostLinear, fade
    animation = workspacesIn,  1,     1.21,  almostLinear, fade
    animation = workspacesOut, 1,     1.94,  almostLinear, fade
    animation = zoomFactor,    1,     7,     quick
}

# Ref https://wiki.hypr.land/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule {
#     name = no-gaps-wtv1
#     match:float = false
#     match:workspace = w[tv1]
#
#     border_size = 0
#     rounding = 0
# }
#
# windowrule {
#     name = no-gaps-f1
#     match:float = false
#     match:workspace = f[1]
#
#     border_size = 0
#     rounding = 0
# }

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# See https://wiki.hypr.land/Configuring/Gestures
gesture = 3, horizontal, workspace

# Example per-device config
# See https://wiki.hypr.land/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bindd = $mainMod, Return, Open terminal, exec, $terminal
bindd = $mainMod, Q, Close active window, killactive,
bindd = $mainMod, C, Calculator, exec, rofi -show calc -modi calc -no-show-match -no-sort
bindd = $mainMod, M, Power menu, exec, pgrep wlogout || ~/.config/wlogout/launch.sh
bindd = $mainMod, E, File manager, exec, $fileManager
bindd = $mainMod, V, Toggle floating, togglefloating,
bindd = $mainMod, F, Fullscreen, fullscreen, 0
bindd = ALT, Tab, Focus next window, cyclenext,
bindd = ALT SHIFT, Tab, Focus previous window, cyclenext, prev
bindd = $mainMod SHIFT, F, Maximize window, fullscreen, 1
bindd = $mainMod, W, Toggle split, layoutmsg, togglesplit
bindd = $mainMod, period, Emoji picker, exec, rofi -show emoji -modi emoji
bindd = $mainMod SHIFT, V, Clipboard history, exec, bash -c 'cliphist list | rofi -dmenu -p "  " -theme ~/.config/rofi/launchers/custom/clipboard.rasi | cliphist decode | wl-copy'
bindd = $mainMod, R, Launch app, exec, ~/.config/rofi/launchers/type-2/launcher.sh || pkill rofi
bindd = $mainMod, slash, Search keybinds, exec, ~/.config/hypr/scripts/search-keybinds.sh
bindd = $mainMod, P, Toggle pseudotile, pseudo,

# Resize windows
bindd = $mainMod CTRL, h, Resize left, resizeactive, -50 0
bindd = $mainMod CTRL, l, Resize right, resizeactive, 50 0
bindd = $mainMod CTRL, k, Resize up, resizeactive, 0 -50
bindd = $mainMod CTRL, j, Resize down, resizeactive, 0 50

# Move floating windows
bindd = $mainMod ALT, h, Move window left, moveactive, -50 0
bindd = $mainMod ALT, l, Move window right, moveactive, 50 0
bindd = $mainMod ALT, k, Move window up, moveactive, 0 -50
bindd = $mainMod ALT, j, Move window down, moveactive, 0 50

# Move focus with mainMod + hjkl
bindd = $mainMod, h, Focus left, movefocus, l
bindd = $mainMod, l, Focus right, movefocus, r
bindd = $mainMod, k, Focus up, movefocus, u
bindd = $mainMod, j, Focus down, movefocus, d

# Swap windows with mainMod + shift + hjkl
bindd = $mainMod SHIFT, h, Swap window left, swapwindow, l
bindd = $mainMod SHIFT, j, Swap window down, swapwindow, d
bindd = $mainMod SHIFT, k, Swap window up, swapwindow, u
bindd = $mainMod SHIFT, l, Swap window right, swapwindow, r

# Switch workspaces with mainMod + [0-9]
bindd = $mainMod, 1, Switch to workspace 1, workspace, 1
bindd = $mainMod, 2, Switch to workspace 2, workspace, 2
bindd = $mainMod, 3, Switch to workspace 3, workspace, 3
bindd = $mainMod, 4, Switch to workspace 4, workspace, 4
bindd = $mainMod, 5, Switch to workspace 5, workspace, 5
bindd = $mainMod, 6, Switch to workspace 6, workspace, 6
bindd = $mainMod, 7, Switch to workspace 7, workspace, 7
bindd = $mainMod, 8, Switch to workspace 8, workspace, 8
bindd = $mainMod, 9, Switch to workspace 9, workspace, 9
bindd = $mainMod, 0, Switch to workspace 10, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bindd = $mainMod SHIFT, 1, Move window to workspace 1, movetoworkspace, 1
bindd = $mainMod SHIFT, 2, Move window to workspace 2, movetoworkspace, 2
bindd = $mainMod SHIFT, 3, Move window to workspace 3, movetoworkspace, 3
bindd = $mainMod SHIFT, 4, Move window to workspace 4, movetoworkspace, 4
bindd = $mainMod SHIFT, 5, Move window to workspace 5, movetoworkspace, 5
bindd = $mainMod SHIFT, 6, Move window to workspace 6, movetoworkspace, 6
bindd = $mainMod SHIFT, 7, Move window to workspace 7, movetoworkspace, 7
bindd = $mainMod SHIFT, 8, Move window to workspace 8, movetoworkspace, 8
bindd = $mainMod SHIFT, 9, Move window to workspace 9, movetoworkspace, 9
bindd = $mainMod SHIFT, 0, Move window to workspace 10, movetoworkspace, 10

# Move window to workspace silently (stay on current workspace)
bindd = $mainMod CTRL, 1, Move window to workspace 1 silently, movetoworkspacesilent, 1
bindd = $mainMod CTRL, 2, Move window to workspace 2 silently, movetoworkspacesilent, 2
bindd = $mainMod CTRL, 3, Move window to workspace 3 silently, movetoworkspacesilent, 3
bindd = $mainMod CTRL, 4, Move window to workspace 4 silently, movetoworkspacesilent, 4
bindd = $mainMod CTRL, 5, Move window to workspace 5 silently, movetoworkspacesilent, 5
bindd = $mainMod CTRL, 6, Move window to workspace 6 silently, movetoworkspacesilent, 6
bindd = $mainMod CTRL, 7, Move window to workspace 7 silently, movetoworkspacesilent, 7
bindd = $mainMod CTRL, 8, Move window to workspace 8 silently, movetoworkspacesilent, 8
bindd = $mainMod CTRL, 9, Move window to workspace 9 silently, movetoworkspacesilent, 9
bindd = $mainMod CTRL, 0, Move window to workspace 10 silently, movetoworkspacesilent, 10

# Cycle workspaces
bindd = $mainMod, Tab, Next workspace, workspace, e+1
bindd = $mainMod SHIFT, Tab, Previous workspace, workspace, e-1
bindd = $mainMod, Backspace, Last workspace, workspace, previous

# Pyprland
bindd = $mainMod, grave, Toggle terminal scratchpad, exec, pypr toggle term
bindd = $mainMod SHIFT, grave, Toggle btop scratchpad, exec, pypr toggle btop
bindd = $mainMod, Z, Toggle zoom, exec, pypr zoom
bindd = $mainMod SHIFT, Z, Zoom in more, exec, pypr zoom ++0.5
bindd = $mainMod, apostrophe, Recover lost windows, exec, pypr lost_windows

# Screenshots with grimblast + satty
bindd = $mainMod SHIFT, S, Screenshot area, exec, grimblast save area - | satty --filename -
bindd = $mainMod SHIFT, P, Screenshot display, exec, grimblast save output - | satty --filename -

# Scroll through existing workspaces with mainMod + scroll
bindd = $mainMod, mouse_down, Next workspace, workspace, e+1
bindd = $mainMod, mouse_up, Previous workspace, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
binddm = $mainMod, mouse:272, Move window, movewindow
binddm = $mainMod, mouse:273, Resize window, resizewindow

# Laptop multimedia keys for volume and LCD brightness
binddel = ,XF86AudioRaiseVolume, Volume up, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
binddel = ,XF86AudioLowerVolume, Volume down, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
binddel = ,XF86AudioMute, Mute audio, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
binddel = ,XF86AudioMicMute, Mute microphone, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
binddel = ,XF86MonBrightnessUp, Brightness up, exec, brightnessctl -e4 -n2 set 5%+
binddel = ,XF86MonBrightnessDown, Brightness down, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
binddl = , XF86AudioNext, Next track, exec, playerctl next
binddl = , XF86AudioPause, Pause playback, exec, playerctl play-pause
binddl = , XF86AudioPlay, Play/pause, exec, playerctl play-pause
binddl = , XF86AudioPrev, Previous track, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrules that are useful

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

windowrule {
    name = float-pavucontrol
    match:class = org.pulseaudio.pavucontrol

    float = yes
    size = 1000 800
}

windowrule {
    name = float-vlc
    match:class = vlc

    float = yes
    size = 1600 900
}

windowrule {
    name = float-swayimg
    match:class = swayimg

    float = yes
    size = 1600 900
}

windowrule {
    name = float-mpv
    match:class = mpv

    float = yes
    size = 1600 900
}

# Pyprland scratchpad windowrules
windowrule {
    name = scratchpad-term
    match:class = scratchpad

    float = yes
    workspace = special:scratch_term
}

windowrule {
    name = scratchpad-btop
    match:class = scratchpad-btop

    float = yes
    workspace = special:scratch_btop
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}
